# listen
PORT=8787

# data persistence
DATA_DIR=./data

# notification mail (required)
ADMIN_EMAIL=staff@example.com

# mail transport (choose one)
# Option A: SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_user
SMTP_PASS=your_pass
MAIL_FROM=no-reply@example.com

# Option B: local sendmail (if SMTP not set, try sendmail)
# SENDMAIL=true
