# ローカル開発用 .env サンプル
# このファイルを .env にコピーして使用してください
# cp .env.local.example .env

# === 必須設定 ===

# セッション署名用シークレット（以下コマンドで生成）
# openssl rand -hex 32
SESSION_SECRET=your-session-secret-here-generate-with-openssl

# === DB設定 ===
# ローカル用DBパス（./data/spform.db を使用）
DB_PATH=./data/spform.db

# === メール設定（MailHog使用時） ===
# MailHog: docker run --rm -p 8025:8025 -p 1025:1025 mailhog/mailhog
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# 送信元・通知先（ローカル用ダミー）
MAIL_FROM=test@example.com
ADMIN_EMAIL=admin@example.com

# メール送信を必須にするか（ローカルではfalse推奨）
MAIL_REQUIRED=false

# === その他 ===
PORT=8787
DATA_DIR=./data
